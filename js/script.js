// let timerId = setInterval(function() {
//     alert( "тик" );
// }, 2000);
$('.slider').slick({
    dots: true,
    // autoplay: true,
    // autoplaySpeed: 5000,
    arrows: false,
    infinite: true,
    speed: 1000,
});
